<!DOCTYPE html>
<html lang="en">

<head></head>

<body style="font-size:20px; padding:50px;">
  <% include ../partials/_header %>
    <ul>
      <% for(var url in urls) { %>
        <li><%= url + ': ' + urls[url] %></li>
        <% }; %>
    </ul>
</body>

</html>
